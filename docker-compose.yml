services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - jeopardy_data:/app/data
    environment:
      NODE_ENV: production
      PORT: "3001"
      DATA_DIR: /app/data
    restart: unless-stopped

volumes:
  jeopardy_data:
